// mockData.ts — Only utility functions remain; all mock data removed.
// Real data is now loaded from OnSpace Cloud (Supabase) via hooks.

export { formatFileSize, formatDate, formatRelativeTime, generateProjectId, generatePreviewUrl } from "./utils";
